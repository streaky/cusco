# Cusco progress and capability evidence

_Last updated: 2026-08-09 on branch `openai-responses-roadmap`._

_Note: many earlier-phase statistics and command outputs are historical snapshots and are intentionally retained for traceability. The per-run artifact files should be treated as point-in-time evidence and may be re-generated by the commands in the section that introduced them._

For current verification, this branch treats the following as the live canonical
evidence artifacts:

- `results/acceptance-report.json` (canonical model-backed behavioral gates and provenance)
- `results/oai-lens-gate.json` (advisory official-SDK conformance delta)
- `results/smoke-report.json` (runtime API, cache, continuation, and compaction evidence)

## Executive summary

Cusco has completed Phases 1 through 10 in `docs/outline.md`:

1. **Phase 1 — executor proof:** a real hybrid/recurrent Gemma checkpoint was captured, displaced, copied through host memory, restored, and continued exactly across four logical contexts.
2. **Phase 2 — Rust logical context store:** Rust represents logical contexts, structurally shared token branches, dependency-aware evaluated-prefix mappings, transactional publication, and logical reference accounting.
3. **Phase 3 — tiered physical manager:** Rust accounts for device, pinned-host, and storage representations; guarded capacity; asynchronous transfer ownership; transactional binding transitions; deterministic eviction; and capacity observability.
4. **Phase 4 — minimal server:** an authenticated HTTP service exposes OpenAI completion/chat adapters and native model/context lifecycle APIs over durable opaque contexts, canonical usage, scheduling, cancellation/deadline handling, and the real llama.cpp executor.
5. **Phase 5 — mapped execution:** transactional device-resident sequence mappings publish by reference, activate without copying checkpoint bytes, and connect committed physical bindings to explicit executor block tables.
6. **Phase 6 — live execution coordinator:** one persistent mapped Gemma executor performs bounded incremental streaming behind count-and-byte FIFO admission, pre-queue transport limits, native cancellation, separate wall and active deadlines, graceful shutdown, and durable-context restart recovery.
7. **Phase 7 — residency and lifecycle scheduling:** dynamic model residency is scheduler-visible, bounded by capacity and policy, with resilient lifecycle transitions and transition-cost-aware execution decisions.
8. **Phase 8 — workload scheduling and operational hardening:** mixed-priority/throughput safeguards and operator controls are in place for sustained control-flow under stress.
9. **Phase 9 — compatibility, persistence, and production packaging:** OpenAI/Ollama/Cusco API cutover, versioned ABI governance, native model catalog persistence, and production-oriented serving entrypoints are implemented.
10. **Phase 10 — semantic context compaction:** v1 baseline deterministic `window_tail` compaction with bounded declarations, correlation metadata, policy-anchored retention, and replay-safe response metadata is implemented and tested.

The Phase 10 Option 2 baseline remains complete. Subsequent OpenAI compatibility
work has added SDK-compatible typed Responses output and streaming, durable
response resources, `previous_response_id` lineage, typed function-call/result
continuation, late output-limit resolution, and explicit `tool_choice` controls.

Cusco remains production-experimental: real vision execution, a Conversations
resource surface, broad family coverage, sustained-load validation, and
formalized multi-model concurrency hardening remain open.


## Capability status

| Capability | Status | Evidence |
|---|---|---|
| Versioned native C ABI around llama.cpp | Demonstrated | The current ABI is version 14; declarations and ownership contracts include checkpointing, token-to-piece conversion, transactional sequence mappings, mapping metrics, execution-profile governance, and the current mapped-state lifecycle. |
| Composite checkpoint capture and restore | Demonstrated on the pinned Gemma artifact | `results/phase1.json` records four restored contexts with non-empty checkpoints. |
| Exact token continuation | Demonstrated | `token_equal: true` for all four contexts in `results/phase1.json`. |
| Bitwise-identical logit continuation | Demonstrated | `logits_equal: true` for all four contexts in `results/phase1.json`; the proof uses exact float-bit comparison rather than tolerance-based comparison. |
| Slot displacement before restore | Demonstrated | The Phase 1 proof replaces each executor's active state with unrelated tokens before restoration and comparison. |
| Device-to-host-to-device checkpoint movement | Demonstrated | `host_round_trip: true` in `results/phase1.json`. |
| Cancellation preserves the active binding | Demonstrated | `cancellation_preserved_binding: true` in `results/phase1.json`. |
| Failed promotion preserves the active binding | Demonstrated | `failed_promotion_preserved_binding: true` in `results/phase1.json`. |
| Concurrent logical-context proof | Demonstrated | Four independently owned executor contexts complete the proof; all four recorded comparisons pass. |
| Locked model identity and digest verification | Implemented and exercised | The proof artifact records the immutable `hf://` identity, expected SHA-256, local path, and file size. The CLI rejects a mismatched digest before executor startup. |
| Persistent/immutable token sequence structure | Implemented and tested | `crates/context-store` uses shared `Arc` sequence nodes; branch tests verify shared prefixes and isolated tails. Here “persistent” describes the immutable data-structure property, not disk persistence. |
| Stable content-derived branch identity | Implemented and tested | Branch IDs are SHA-256-derived from the prior branch hash, token, and fixed-width position encoding, so identities are portable across 32-bit and 64-bit targets. |
| Evaluated-prefix dependency identity | Implemented and tested | Mapping identity includes model epoch, adapter epoch, parent mapping, branch prefix, fixed-width represented end, required components, evaluation parameters, and lineage hash. |
| Longest valid evaluated-prefix lookup | Implemented and tested | Lookup uses an epoch-and-branch index, then checks branch identity, required checkpoint components, and the complete parent dependency chain without materializing token vectors during validation. |
| Transactional mapping publication | Implemented and tested | Publication is prepared separately and committed only if the context's active mapping is unchanged; stale concurrent publication returns `PublicationConflict`. |
| Logical reference accounting and reclamation | Implemented and tested | Catalog, context, and dependent references prevent premature reclamation; dependency-chain cleanup is iterative so reclamation depth does not consume call stack. |
| Per-file Rust coverage enforcement | Active | `tools/coverage.sh` generates workspace coverage and `tools/check_coverage.py` rejects any measured Rust source file below 80% line coverage. |
| Physical device/host/storage tier manager | Implemented and tested | `crates/physical-manager` accounts for residency and guarded capacity, models transfers separately from prepared transitions, commits bindings revision-transactionally, and emits capacity metrics and structured trace events. |
| Durable cross-process context persistence | Implemented and tested | Phase 4 atomically persists the model catalog, logical contexts, branches, revisions, and opaque UUIDs; restart tests verify recovery and non-reuse. Physical representation storage remains separate ownership/accounting metadata. |
| Inference server, scheduler, streaming API | Implemented and GPU-exercised | `cusco-server` provides bounded admission, transition-cost selection, OpenAI completion/chat adapters, native lifecycle routes, shared stream events, cancellation/deadline handling, canonical usage, and checked OpenAPI. `results/phase4-server.json` records a real-model authenticated streaming run. |
| Mapped device-resident execution | Implemented and GPU-exercised | `results/phase5.json` records four exact mapped branch continuations, reference-only activation, graph epoch stability, staged-versus-mapped timing, bytes moved, and prompt work avoided. |
| Persistent mapped live execution | Implemented and GPU-exercised | The server keeps one validated model and executor slot alive, reuses complete mapped prefixes, and reports separate tokenization, prefix lookup, activation, uncached-prefill, transfer, device, and host accounting in `results/phase6c-server.json`. |
| Incremental bounded streaming | Implemented and GPU-exercised | Request-owned sampling and token rendering feed a bounded event channel; the Phase 6C artifact confirms visible stream tokens remain bounded by sampled tokens and terminal usage is emitted. |
| Bounded FIFO lifecycle admission | Implemented and tested | Admission is bounded to 32 queued requests and 16 MiB retained bytes after pre-queue limits and model resolution. Overload returns canonical HTTP 429 `queue_overloaded` with `Retry-After: 1`. |
| Native cancellation and separate deadlines | Implemented and GPU-exercised | Cancellation uses llama.cpp's atomic abort callback; wall and active deadline watchdogs terminate owned work without publishing a successor context. |
| Graceful shutdown and restart recovery | Implemented and GPU-exercised | SIGTERM stops admission, drains for up to 30 seconds, then coherently cancels remaining work. Restart recovers durable contexts while queue entries and live request handles remain ephemeral. |
| Deterministic semantic compaction | Implemented and server-tested | Phase 10 baseline enforces a deterministic, anchor-aware `window_tail` strategy, bounded declaration lifecycle, principal-scoped operator limits, and replay metadata (`cusco.compaction_result`) on terminal Responses. |
| Durable typed Responses API | Implemented and model-exercised | Stored-resource retrieval/deletion/cancellation, restart recovery, `previous_response_id`, SDK-reconstructable streaming, and typed function-call/result continuation are covered by the canonical acceptance and advisory oai-lens gates. |
| Responses `tool_choice` controls | Implemented and tested | `auto`, `none`, `required`, and named-function choices are validated before generation; invalid modes and undeclared named functions return stable `invalid_request` errors. |
| OpenAI SDK conformance | 6 of 7 advisory probes pass | `results/oai-lens-gate.json` records Chat and Responses buffered/streaming passes plus tool result continuation. The sole unchanged failure is real `vision.data_url` execution. |

## Phase 1 real-model proof

### Validated model

The locally retained proof artifact identifies the external validation model as:

- Identity: `hf://models/unsloth/gemma-4-E2B-it-GGUF@0314792d7f1f7e229411f620751375812bb9faf2/gemma-4-E2B-it-Q3_K_M.gguf`
- SHA-256: `90293b8cdaf9c973012bf4df8a1e92bde7d74ad66a4fe56cf905ccd563d660c5`
- Size: `3,356,037,216` bytes
- Reported vocabulary: `262,144` tokens
- Reported checkpoint components: global KV, sliding-window attention state, and recurrent state
- llama.cpp source tag: read from the repository-wide `llama.cpp-version.txt` source of truth

Model weights are external and are not committed to the repository.

### Recorded integration report

The machine-readable evidence is `results/phase1.json`. The human-readable report is generated and asserted by `tools/report-inference-proof.py`.

| Context | Prompt | Input token | Inferred next token | Checkpoint bytes | Checksum | Exact after restore |
|---:|---|---:|---:|---:|---:|:---:|
| 0 | `The capital of France is [0]` | 236,842 | 107 | 166,506 | 15,855,946,795,732,230,871 | PASS |
| 1 | `The capital of France is [1]` | 236,842 | 9,079 | 166,506 | 7,424,184,562,009,432,005 | PASS |
| 2 | `The capital of France is [2]` | 236,842 | 9,079 | 166,506 | 10,193,984,636,459,084,218 | PASS |
| 3 | `The capital of France is [3]` | 236,842 | 9,079 | 166,506 | 17,792,484,377,050,697,218 | PASS |

Additional observed outcomes:

- Four independent contexts: **PASS**
- Real model vocabulary available (`262,144` tokens): **PASS**
- Restored tokens identical in every context: **PASS**
- Restored logits bitwise-identical in every context: **PASS**
- Device-to-host-to-device round trip: **PASS**
- Cancellation preserved the prior binding: **PASS**
- Deliberately failed restore preserved the prior binding: **PASS**
- Total proof time: **100,009 ms**
- CUDA device: **NVIDIA GeForce GTX 1080 Ti**, compute capability 6.1
- Model placement: **36/36 layers offloaded to GPU**

The differing checksums demonstrate four distinct captured states. The different next-token IDs also show that the report records real prompt-dependent model evaluation rather than only exercising checkpoint serialization.

### Reproduction command

With the verified model at `models/gemma-4-e2b-it.gguf`, Docker's NVIDIA runtime configured, and GPU 1 available:

```sh
tools/inference-integration-test.sh
```

The script rebuilds the current executor image, runs the Compose proof service, verifies the configured model SHA-256, confirms GPU visibility, writes `results/phase1.json`, validates every assertion, prints the concise Markdown report above, and exits nonzero on failure. `CUSCO_GPU_DEVICE_ID`, `CUSCO_MODEL_DIR`, and `CUSCO_RESULT_DIR` override the defaults.

## Phase 2 logical-context-store evidence

The `cusco-context-store` crate implements:

- logical context IDs independent of execution slots;
- immutable token sequences with structurally shared prefix nodes;
- cheap branching at any valid token boundary;
- stable SHA-256-derived branch, lineage, and evaluated-prefix identities;
- model and adapter epochs that invalidate incompatible evaluated state;
- composite checkpoint masks for global KV, SWA, and recurrent components;
- parent-linked evaluated-prefix mappings;
- deterministic longest-valid-prefix selection through an epoch-and-branch index;
- two-step prepared/committed publication;
- conflict rejection when another publication wins first; and
- catalog, active-context, and dependent reference counts with iterative reclamation.

The focused test suite currently has nine tests, including a property test over arbitrary branch prefixes. On 2026-08-05 the following command passed:

```sh
cargo +1.85.1 test -p cusco-context-store
```

Observed result: **9 passed, 0 failed**.

The tests exercise:

- exact shared-prefix structure and private branch tails;
- arbitrary prefix preservation;
- atomic publication and epoch invalidation;
- stale concurrent-publication rejection;
- longest-valid-prefix behavior across diverging branches;
- dependency-aware reference reclamation; and
- rejection of incomplete, out-of-bounds, and invalid-parent publications.

## Phase 3 tiered-physical-manager evidence

The new `cusco-physical-manager` crate implements:

- content-independent physical representation IDs and explicit device, pinned-host, and storage residency;
- separate logical, active-binding, transition-reservation, transfer, and growth-reservation ownership;
- exact composite validation across global KV, sliding-window attention, and recurrent components;
- guarded growth and transition capacity plus opportunistic warm-device capacity;
- reference-only, non-destructive, quiesced, and recompute transition classes;
- asynchronous transfer completion whose ownership survives transition cancellation;
- revision-guarded atomic binding commits that preserve the prior binding on failure;
- device-to-host demotion and host/storage-to-device promotion;
- deterministic value/LRU warm-state eviction; and
- capacity metrics and structured prepared, committed, aborted, transfer, promotion, demotion, recomputation, and eviction events.

The focused suite passed **9 tests, 0 failed**. It covers guarded admission, deterministic eviction, host promotion and demotion, reference-only switching, stale revision rejection, failed and cancelled transitions, detached-transfer capacity reservation, transfer lifetime, recomputation, explicit unbinding/reference release, and composite completeness and boundary agreement.

## Phase 4 minimal-server evidence

The `cusco-server` crate and `cusco serve` command implement:

- authenticated OpenAI-compatible completion, chat, and model-list routes;
- native administrative model lifecycle and durable context/branch/import APIs;
- loopback-safe anonymous development mode and bearer authentication for public listeners;
- bounded admission and deterministic transition-cost slot selection;
- transactional durable context commits with cancellation and deadline rejection;
- canonical usage records and a shared started/token/usage/finished event sequence;
- checked OpenAPI publication; and
- real llama.cpp token generation and token-to-piece conversion through the versioned C ABI, currently version 3.

The historical Phase 4 machine-readable result is `results/phase4-server.json`. That recorded GTX 1080 Ti run generated 72 tokens for a distributed-checkpoint prompt in 2,541 ms of server-reported latency and observed its first then-buffered SSE token at 2,542.35 ms. It demonstrated authenticated registration and lookup, required OpenAPI routes, token/usage/finished events, and model revision and timing. The Phase 6C workflow supersedes the removed Phase 4-only report runner and exercises the current persistent, incremental server path.

## Phase 5 mapped-execution evidence

The native ABI and safe Rust wrapper expose prepare/commit mapping forks,
activation, removal, and metrics. Publication is transactional: dropped
preparations are invisible, failed commits leave the prior active mapping
valid, and committed mappings remain independently addressable. The physical
manager publishes a `DeviceBlockTable` only after the corresponding physical
binding has committed and removes stale tables on transition or unbind.

The reproducible GPU proof is:

```sh
docker compose run --rm mapped-proof
```

The latest observed GTX 1080 Ti run continued four mapped branches with token
ID `26182` in every branch. Four reference switches copied **0 activation
bytes**. Four staged restores read **668,040 bytes** and took **5,944,233 ns**,
while four mapped activations took **2,400 ns** and avoided reevaluating **36
prompt tokens**. These are focused single-run observations, not sustained-load
benchmarks; mapping fork creation inside llama.cpp may still copy KV/recurrent
sequence data, while subsequent activation is reference-only. The proof does
not claim graph/cache reuse because llama.cpp exposes no public rebuild signal.

## Phase 6 live-coordinator evidence

Phase 6 is delivered as three cumulative milestones:

- **6A — persistent mapped core:** one schema-validated Gemma execution profile, process-owned model and executor slot, mapped activation, complete-block publication, and exact continuation bounds;
- **6B — incremental generation frontier:** one-token decode quanta, request-owned greedy sampling, bounded stream events, incremental UTF-8 and stop matching, presentation normalization, finish reasons, and canonical usage;
- **6C — bounded lifecycle and integrated proof:** fixed count-and-byte FIFO admission, pre-queue transport limits, cancellation/disconnect propagation, separate wall and active deadlines, graceful shutdown, restart recovery, and the complete acceptance artifact.

### Recorded data summary

| Evidence | GPU | Initial request | Continuation/cache result | Acceptance |
|---|---|---|---|---|
| Phase 6A historical snapshot | Tesla PG500-216 (`sm_70`) | 41 token events; 43 total stream events | Transitional reporter did not yet capture canonical usage or a successful continuation | Historical data only; cumulative behavior is accepted by 6B/6C |
| Phase 6B generation frontier | GTX 1080 Ti (`sm_61`) | 37 input tokens; 40 generated tokens; 595 ms server latency; 596.36 ms client wall time; 104.48 ms first token; 102.13 ms measured prompt processing | 64 of 84 prompt tokens cached (76.19%); 20 evaluated; 8 generated; 112 ms server latency; 25.24 ms first token; 23.40 ms prompt processing | 13/13 report checks passed |
| Phase 6C integrated lifecycle | Tesla PG500-216 (`sm_70`) | 37 input/evaluated tokens; 42 generated tokens; 937 ms server latency; 162.09 ms prompt processing | 64 of 86 prompt tokens cached (74.42%); 22 evaluated; 9 generated; 178 ms server latency; 29.30 ms first token; 27.55 ms prompt processing | 17/17 integrated checks passed |

Each live Phase 6C request reported **294,912 transfer bytes**, while the run recorded **8 GiB peak device accounting** and **294,912 bytes peak host accounting**. Its mapped proof switched references four times with **0 activation bytes copied**, avoided reevaluating **36 prompt tokens**, and compared those activations with **668,040 staged bytes read**.

The fixed lifecycle envelope exercised by Phase 6C is:

| Limit | Recorded value |
|---|---:|
| Active native execution slots | 1 |
| FIFO queue count | 32 requests |
| FIFO retained-byte capacity | 16 MiB |
| Per-request body capacity | 1 MiB |
| Concurrent pre-queue operations | 16 |
| Header capacity | 32 KiB |
| Body-read timeout | 10 s |
| Wall deadline | 300 s |
| Active deadline | 240 s |
| Graceful-shutdown drain | 30 s |

The integrated check matrix passed exact isolated continuation, prior-binding preservation after native failures, reference-only mapped activation, bounded streaming with terminal usage, canonical retryable overload, real cancellation and deadline termination without context publication, graceful SIGTERM shutdown, and durable-context recovery without ephemeral queue recovery.

### Reproduction

```sh
CUSCO_GPU_DEVICE_ID=0 tools/phase6c-report.sh
```

The workflow runs the GPU-less per-file coverage gate, exact real-model checkpoint proof, mapped staged-versus-reference-only proof, and live Phase 6C server matrix. The generated `results/phase6c-server.json` records configuration, provenance, selected GPU, exactness, prompt timings, cache work, accounting, terminal-state injections, shutdown, restart, and coverage. Generated result files remain local artifacts and are not committed.
### Phase 10 smoke-report: usage + continuity telemetry

The current deterministic smoke runner now captures request-level and aggregate cache/continuation telemetry in `results/smoke-report.json` and includes it in every run:

- `cusco_usage.input_tokens`
- `cusco_usage.generated_tokens`
- `cusco_usage.evaluated_tokens`
- `cusco_usage.cached_tokens`
- `cusco_usage.prefill` split (`cached_tokens`, `uncached_tokens`, `cached_ratio`, and per-stage nanosecond timings)
- Aggregate `stats`: `cached_tokens`, `evaluated_tokens`, `prefill_cached_tokens`, `prefill_uncached_tokens`, and derived `cache_ratio`

Smoke scenarios now include continuity probes (`context_reuse_seed` and `context_reuse_followup`) plus continuation after compaction (`compacted_successor_continuation`) so cache reuse and successor behavior are preserved in the same artifact.

Latest successful run on 2026-08-08 produced:

- **13 scenarios**, **13 passed**, **0 failed**
- Aggregate cache ratio in stats: **0.428571**
- Included for trend analysis across runs: per-request `cusco_usage.prefill.cached_ratio` and request-level `prefill` timings.

Run command:

```sh
docker compose -f compose.test.yaml run --rm --no-deps --build api-smoke
```

Primary evidence artifact:

```text
results/smoke-report.json
```

Phase 10 v1 baseline behavior is now represented as a cohesive in-tree set of contracts and tests:

- deterministic `window_tail` policy-aware trimming,
- bounded and principal-scoped compaction declarations with bounded concurrency/rate controls,
- anchor-preserving no-retention fallback semantics,
- OpenAI Responses replay metadata (`cusco.compaction_result`) on terminal responses,
- model-sequence terminal-sequence suppression compatibility behavior, and
- debug-path safety for body capture and timing observability.

### Latest verification snapshot (2026-08-08)

```sh
docker compose -f compose.test.yaml run --rm test cargo test -p cusco-server
```

Result: **100 tests passed, 0 failed, 0 ignored**.

This run included the latest Phase 10 checks, including:

- `context_strategy::tests::window_tail_recognizes_all_system_anchor_formats`
- `context_strategy::tests::window_tail_fallback_retains_whole_turn_when_budget_is_too_small`
- `tests::compaction_creation_times_prune_stale_principals`
- `tests::compaction_operator_limits_bound_workers_declarations_and_rate`
- `tests::compaction_result_payload_for_openai_replay`
- `tests::compaction_success_successor_commit`
- `tests::semantic_regression_followup_fidelity`
- `tests::semantic_regression_tool_call_consistency`

Fixture-backed semantic assertions run from `crates/server/tests/fixtures/phase10_semantic.json`.

Smoke-report failure-path robustness was also re-verified:

```sh
CUSCO_SMOKE_BASE_URL=http://127.0.0.1:9 CUSCO_SMOKE_OUTPUT=/tmp/smoke-report.json python tools/smoke-report.py
```

That probe returns structured per-scenario failure payloads and exits with code 1 instead of crashing, while still reporting deterministic timing/status metadata.

```sh
cat /tmp/smoke-report.json | sed -n '1,6p'
```

```json
{
  "schema_version": 1,
  "deterministic": true,
  "base_url": "http://127.0.0.1:9",
  "scenarios": [
```

The full JSON includes all nine probe scenarios and confirms `passed: false` with `error: "expected HTTP 200, got 0"` at transport failures.


## OpenAI Responses compatibility evidence

The 2026-08-09 compatibility cycle completed the portable Responses core:

- SDK-compatible buffered output items and `output_text`;
- typed, reconstructable text and function-call streaming events;
- preserved omitted output limits with late context-headroom resolution;
- file-backed stored-resource recovery, quotas, cleanup, retrieval, deletion,
  cancellation, and `previous_response_id` replay;
- typed client-side function-call/result continuation; and
- precise `tool_choice` validation for `auto`, `none`, `required`, and named
  functions.

The canonical real-model command was:

```sh
docker compose -f compose.test.yaml run --rm acceptance
```

`results/acceptance-report.json` records `passed: true` for every canonical gate
with complete model, workload, runtime, and device provenance. The advisory
official-SDK gate recorded **6 passed / 1 failed** in
`results/oai-lens-gate.json`; the only unchanged failure is `vision.data_url`,
because admitted image bytes are not yet projected by the executor. No probe
newly failed.

## Test and coverage evidence

The repository's containerized gate is:

```sh
docker compose -f compose.test.yaml build test
docker compose -f compose.test.yaml run --rm test
```

The most recent observed full gate on 2026-08-05 passed all runnable workspace tests, including **28 server tests**. The Phase 6C workflow ran this GPU-less coverage gate before the external-model GPU proofs.

Latest cycle validation (2026-08-08):

```sh
docker compose -f compose.test.yaml run --rm test cargo test -p cusco-server
```

Result: **100 tests passed, 0 failed, 0 ignored**, with warnings only.


Per-file line coverage reported by that run:

| Rust source file | Line coverage |
|---|---:|
| `crates/cli/src/main.rs` | 82.40% |
| `crates/context-store/src/lib.rs` | 97.08% |
| `crates/executor/src/lib.rs` | 95.62% |
| `crates/model-registry/src/lib.rs` | 92.86% |
| `crates/physical-manager/src/lib.rs` | 98.82% |
| `crates/server/src/generation.rs` | 99.21% |
| `crates/server/src/lib.rs` | 95.93% |
| `crates/server/src/mapped.rs` | 87.89% |

Every measured Rust source file exceeded the required **80%** threshold.

## Reproducibility and build constraints

- `llama.cpp-version.txt` is the sole llama.cpp version source and contains a release tag rather than a duplicated commit hash.
- Docker and executor fetch/verification tooling consume that version file.
- Local CUDA builds default to `sm_61` and `sm_70`, covering the GTX 1080 Ti and Tesla GV100-class devices used for development.
- Broader CUDA architecture lists remain configurable through `CUSCO_CUDA_ARCHITECTURES` for release builds.
- CPU-only tests use the same CUDA-capable image without GPU passthrough, with CUDA's stub driver library available for linking/loading.
- Model assets and generated proof results are mounted separately from the image build and are not required for model-free lifecycle and logical-store tests.
## Evidence index
| Evidence | Location |
|---|---|

| Phase 10 semantic-regression fixtures | `crates/server/tests/fixtures/phase10_semantic.json` |
| Phase 10 cache/continuity smoke telemetry artifact | `results/smoke-report.json` |
| Phase 1 machine-readable real-model result | `results/phase1.json` |
| Phase 1 integration scenario | `crates/executor/tests/phase1.rs` |
| Proof orchestration and result generation | `crates/cli/src/main.rs` |
| Safe Rust executor/checkpoint API | `crates/executor/src/lib.rs` |
| Native ABI contract | `native/include/cusco_executor.h` |
| Native llama.cpp implementation | `native/shim/cusco_executor.cpp` |
| Logical context store and behavioral tests | `crates/context-store/src/lib.rs` |
| Tiered physical manager and behavioral tests | `crates/physical-manager/src/lib.rs` |
| Runnable real-model checkpoint report | `tools/inference-integration-test.sh`, `tools/report-inference-proof.py` |
| Phase 4 machine-readable real-model server result | `results/phase4-server.json` |
| Phase 5 machine-readable mapped proof | `results/phase5.json` |
| Phase 6A historical persistent-core result | `results/phase6a-server.json` |
| Phase 6B machine-readable generation-frontier result | `results/phase6b-server.json` |
| Phase 6C machine-readable integrated result | `results/phase6c-server.json` |
| Historical Phase 6C artifacts | `results/phase6c-server.json`, `results/phase6c-state.json`, `results/phase6c-shutdown.json` |
| Mapped executor and block-table behavior | `native/shim/cusco_executor.cpp`, `crates/executor/src/lib.rs`, `crates/physical-manager/src/lib.rs`, `crates/server/src/mapped.rs` |
| Live server, HTTP adapters, scheduler, persistence, lifecycle, and tests | `crates/server/src/lib.rs`, `crates/server/src/generation.rs` |
| Immutable model registry | `crates/model-registry/src/lib.rs` |
| Containerized proof/test services | `compose.test.yaml` |
| Per-file coverage gate | `tools/coverage.sh`, `tools/check_coverage.py` |
| llama.cpp release tag source | `llama.cpp-version.txt` |
| Full phased design and acceptance criteria | `docs/outline.md` |
| Canonical real-model acceptance report | `results/acceptance-report.json` |
| Advisory OpenAI SDK-conformance report and delta | `results/oai-lens-report.json`, `results/oai-lens-gate.json` |

## Current boundary and next gate

The evidence now supports deterministic compaction, bounded residency and
scheduling, native execution, durable typed Responses resources, SDK-compatible
stream reconstruction, stored lineage continuation, and client-executed
function-call/result loops.

It does not establish full OpenAI Responses parity. Real image projection,
Conversations, the broader reasoning/hosted-tool/item taxonomy, detached
background execution, sustained-load characterization, broad model-family
coverage, and hardened concurrent multi-model execution remain open.

The next API-depth gate is Conversations or stronger structured-output
conformance; the next executor capability gate is real vision with the pinned
Gemma model.
