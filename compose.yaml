services:
  test:
    build: .
    command: ["tools/coverage.sh"]
  executor-proof:
    build: .
    command: ["sh", "-c", "nvidia-smi --query-gpu=uuid --format=csv,noheader && cargo run -p cusco -- proof /models/gemma-4-e2b-it.gguf --sha256 90293b8cdaf9c973012bf4df8a1e92bde7d74ad66a4fe56cf905ccd563d660c5"]
    environment:
      CUSCO_REQUESTED_HOST_GPU: "${CUSCO_GPU_DEVICE_ID:-1}"
      LD_LIBRARY_PATH: "/opt/llama-build/bin"
    volumes: ["${CUSCO_MODEL_DIR:-./models}:/models:ro", "${CUSCO_RESULT_DIR:-./results}:/results"]
    deploy: { resources: { reservations: { devices: [{ driver: nvidia, device_ids: ["${CUSCO_GPU_DEVICE_ID:-1}"], capabilities: [gpu] }] } } }
  mapped-proof:
    build: .
    command: ["sh", "-c", "nvidia-smi --query-gpu=uuid,name,compute_cap --format=csv,noheader > /results/phase5-gpu.csv && cargo run -p cusco -- mapped-proof /models/gemma-4-e2b-it.gguf"]
    environment:
      CUSCO_REQUESTED_HOST_GPU: "${CUSCO_GPU_DEVICE_ID:-1}"
      LD_LIBRARY_PATH: "/opt/llama-build/bin"
    volumes: ["${CUSCO_MODEL_DIR:-./models}:/models:ro", "${CUSCO_RESULT_DIR:-./results}:/results"]
    deploy: { resources: { reservations: { devices: [{ driver: nvidia, device_ids: ["${CUSCO_GPU_DEVICE_ID:-1}"], capabilities: [gpu] }] } } }
  server-report:
    build: .
    command:
      - sh
      - -c
      - >-
        chmod a+rwx /results &&
        nvidia-smi --query-gpu=uuid,name,compute_cap --format=csv,noheader
        > /results/phase6a-gpu.csv &&
        cargo run -p cusco -- serve /models/gemma-4-e2b-it.gguf --listen 0.0.0.0:8080
        --state /results/phase6a-state.json
        --bearer-token phase6a-report-token
    environment:
      CUSCO_REQUESTED_HOST_GPU: "${CUSCO_GPU_DEVICE_ID:-1}"
      LD_LIBRARY_PATH: "/opt/llama-build/bin"
    ports: ["127.0.0.1:${CUSCO_SERVER_REPORT_PORT:-18082}:8080"]
    volumes: ["${CUSCO_MODEL_DIR:-./models}:/models:ro", "${CUSCO_RESULT_DIR:-./results}:/results"]
    deploy: { resources: { reservations: { devices: [{ driver: nvidia, device_ids: ["${CUSCO_GPU_DEVICE_ID:-1}"], capabilities: [gpu] }] } } }
