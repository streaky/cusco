[upstream]
repository = "https://github.com/ggml-org/llama.cpp.git"
[build]
profile = "release"
cmake_options = ["GGML_CUDA=OFF", "LLAMA_BUILD_TESTS=OFF", "LLAMA_BUILD_EXAMPLES=OFF"]
[patches]
manifest = "patches/series.toml"
